#encoding: ISO-8859-1
-if !Rails.env.vbox?
  = link_to gravatar_for(current_user,'15px'),user_url(current_user.id)
-#= link_to (user_url(current_user), = gravatar_for (current_user))
%h1=current_user.name
-#%p= link_to "Ver mi perfil", current_user
.span
  -#pluralize(current_user.microposts.count, "comentarios")
