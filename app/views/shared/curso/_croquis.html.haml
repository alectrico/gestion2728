#columns
  .column{:draggable => :false,:style => "overflow:hidden;height: 150px; width: 150px; float:left; border: 2px solid #666666; background-color: #ccc; margin-right: 5px; -webkit-border-radius: 10px; -ms-border-radius: 10px; -moz-border-radius: 10px; border-radius: 10px; -webkit-box-shadoww: inset 0 0 3px #000; -ms-box-shadow: inset 0 0 3px #000; box-shadow: inset 0 0 3px #000; text-align: center;"}
    -foto=Foto.find_by_id(f.object.te1_foto_id)
    -if !foto.nil?      
      -id= f.object.te1_foto_id
      -if foto.descripcion.include?('CROQUIS')
        -cover = Foto.find_by_descripcion("CUBIERTA CROQUIS")
        =image_tag (code_image_foto_url(cover))
      -else
        =image_tag(code_image_foto_url(id))
 

%noscript
  :css  
    .column .over{ border: 4px dashed #000;  }

%script
  $(document).ready(function(){
  $(".column").on("click",function(event){
  $(this).parent().parent().find("#croquis_foto_id input").css("background-color","LightYellow");

  $(this).parent().parent().find("#croquis_foto_id input").val( $(this).text());
  -#$(this).parent().parent().find("#te1_name input").val( $(this).text());
  -#$(this).parent().parent().find("#te1_url input").val( $(this).text());
 

  /val( $(this).text());
  
  /alert('on click');
  });
  });












  var cols = document.querySelectorAll('#columns .column');
  [].forEach.call(cols, function(col) {
  /col.addEventListener('dragstart', handleDragStart, false);
  /col.addEventListener('dragenter', handleDragEnter, false)
  col.addEventListener('dragover', handleDragOver, false);
  /ol.addEventListener('dragleave', handleDragLeave, false);
  col.addEventListener('drop', handleDrop, false);
  col.addEventListener('dragend', handleDragEnd, false);
  });



