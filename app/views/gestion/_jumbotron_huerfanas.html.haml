.container
  .row.row-offcanvas.row-offcanvas-left
    .col-12.col-md-9
      %p.float-right.hidden-md-up
        -#%button.btn.btn-primary.btn-sm{"data-toggle" => "offcanvas", :type => "button"} Vincular
        -unless  item.auditoria
          -#.col-10.col-md-10
          = form_for item do |f|
            = f.hidden_field :auditoria_id , :value => @gestion_auditoria.id, :class => "form-control"
            = f.submit "Vincular"




      .jumbotron
        %tr
          = content_for?(:contenido) ? yield(:contenido) : yield

    #sidebar.col-6.col-md-3.sidebar-offcanvas
      -#= render 'sidebar'

  %p
    = item.descripcion
  %hr/
